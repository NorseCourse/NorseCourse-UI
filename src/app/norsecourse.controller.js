(function() {
    'use strict';

    /**
     * @ngdoc controller
     * @name norseCourse.controller:main
     * @description
     *
     * Main controller for NorseCourse app, which doesn't actually do anything
     *
     */
    angular.module('norseCourse').controller('main',function($scope) {
    });
})();

